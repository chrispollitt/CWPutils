* for dumb splitting kernels, rejoin and split properly!

* add flag for "don't make allowances for non-hashbang calls"
   - right now, if the interpreter flags are pre-split, it is handled
* change c code to c++
* fix parse_flags
   - make it generic - NO!
   - replace with getopts_long
   - replace with boost program_options
   - other?
* use namespace cwputils
* flags doen't need to be global either
* fix man page:
  - autoformat middle in Makefile
  - manually format head and foot
